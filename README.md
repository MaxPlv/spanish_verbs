# Spanish Verbs Telegram Bot

Telegram-бот для изучения испанских глаголов с ежедневными квизами и отправкой форм глаголов по расписанию.

## 📋 Возможности

- **Глагол дня**: Каждый день в 09:00 бот выбирает случайный глагол из 300 самых популярных испанских глаголов
- **Квизы**:
  - 10:00 — Квиз на перевод (инфинитив → русский)
  - 11:00 — Квиз на инфинитив (русский → инфинитив)
- **Формы глаголов**: С 13:00 до 23:00 каждый час бот отправляет формы глагола в разных временах
- **Многопользовательский режим**: Каждый пользователь имеет свой независимый цикл обучения

## 🛠 Установка

### 1. Клонируйте репозиторий или скачайте файлы

```bash
cd spanish_verbs
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота

### 4. Настройте переменные окружения

Создайте файл `.env` или установите переменную окружения:

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
```

Или в `.env` файле:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### 5. Убедитесь, что файл `verbs.csv` находится в директории проекта

## 🚀 Запуск

```bash
python bot.py
```

Бот запустится в режиме polling и будет ожидать сообщения.

## 📱 Использование

### Команды

- `/start` — Начать работу с ботом
- `/status` — Показать текущий глагол дня

### Процесс обучения

1. **09:00** — Бот отправляет глагол дня с переводом
2. **10:00** — Квиз: выбери правильный перевод испанского глагола
3. **11:00** — Квиз: выбери правильный инфинитив по русскому переводу
4. **13:00-23:00** — Каждый час бот отправляет формы глагола в одном из времён:
   - Presente
   - FuturoSimple
   - PreteritoIndefinido
   - И другие времена из CSV

## 📂 Структура проекта

```
spanish_verbs/
│
├── bot.py                 # Основной файл бота
├── data_loader.py         # Загрузка и работа с CSV данными
├── state_manager.py       # Управление состоянием пользователей (SQLite)
├── quiz_generator.py      # Генерация квизов
├── verbs.csv              # База данных глаголов
├── requirements.txt       # Зависимости Python
├── README.md              # Документация
└── bot_state.db          # База данных SQLite (создаётся автоматически)
```

## ⚙️ Настройка часового пояса

По умолчанию используется часовой пояс `Europe/Moscow`. Чтобы изменить его, отредактируйте переменную `TIMEZONE` в файле `bot.py`:

```python
TIMEZONE = pytz.timezone('Europe/Moscow')  # Измените на свой часовой пояс
```

Список доступных часовых поясов: [pytz timezones](https://gist.github.com/heyalexej/8bf688fd67d7199be4a1682b3eec7568)

## 🔧 Развёртывание

### Локальный запуск (Polling)

Бот использует polling режим по умолчанию — подходит для разработки и тестирования.

### Railway.app (Рекомендуется)

Railway — простой способ развернуть бота в облаке:

#### Шаг 1: Подготовка проекта

Убедитесь, что все файлы созданы (уже есть в проекте):
- ✅ `Procfile`
- ✅ `railway.json`
- ✅ `runtime.txt`
- ✅ `requirements.txt`

#### Шаг 2: Деплой на Railway

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект: **New Project** → **Deploy from GitHub repo**
3. Подключите ваш GitHub репозиторий (или используйте **Deploy from local**)
4. Railway автоматически определит Python проект и начнёт деплой

#### Шаг 3: Настройка переменных окружения

В Railway dashboard:
1. Откройте ваш проект
2. Перейдите в **Variables**
3. Добавьте переменную:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
   ```
4. Сохраните изменения

#### Шаг 4: Запуск

Railway автоматически перезапустит бота с новыми переменными. Проверьте логи в разделе **Deployments**.

#### Альтернатива: Деплой через CLI

```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти в аккаунт
railway login

# Инициализировать проект
railway init

# Добавить переменную окружения
railway variables set TELEGRAM_BOT_TOKEN=ваш_токен

# Задеплоить
railway up
```

### Другие платформы

1. **Heroku**:
   - Создайте приложение на Heroku
   - Добавьте переменные окружения
   - Используйте polling режим

2. **Google Cloud Run**:
   - Соберите Docker-образ
   - Разверните на Cloud Run
   - Настройте webhook URL

3. **VPS/Dedicated Server**:
   - Используйте systemd для автозапуска
   - Настройте nginx как reverse proxy

### Пример systemd service

Создайте файл `/etc/systemd/system/spanish-bot.service`:

```ini
[Unit]
Description=Spanish Verbs Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/spanish_verbs
Environment="TELEGRAM_BOT_TOKEN=your_token"
ExecStart=/usr/bin/python3 /path/to/spanish_verbs/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable spanish-bot
sudo systemctl start spanish-bot
```

## 📊 Структура CSV файла

CSV файл должен содержать следующие столбцы:

```
popularity,infinitivo,translation_ru,Presente__1s,Presente__2s,...
```

Где:
- `popularity` — рейтинг употребления (1-100)
- `infinitivo` — инфинитив глагола
- `translation_ru` — перевод на русский
- `Время__Лицо` — формы глагола (например, `Presente__1s`, `FuturoSimple__2p`)

## 🐛 Отладка

Логи выводятся в консоль. Для более подробных логов можно изменить уровень:

```python
logging.basicConfig(level=logging.DEBUG)
```

## 📝 Лицензия

MIT

## 🤝 Вклад

Буду рад вашим pull requests и issues!
